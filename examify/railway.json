{
  "$schema": "https://schema.railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd examify/frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd examify/backend && pip install -r requirements.txt && python app.py",
    "restartPolicyType": "ON_FAILURE"
  }
}